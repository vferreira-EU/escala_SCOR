<!DOCTYPE html>
<html lang="pt">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PGIL - Smart City of Rock</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
<style>
:root{
--bg:#0b0f14;--card:#151b22;--gold:#D4AF37;--green:#2ECC71;--blue:#00c2ff;--text:#fff;--muted:#9aa6b2;
}
*{box-sizing:border-box} body{margin:0;font-family:Inter,sans-serif;background:
radial-gradient(circle at top left,rgba(0,194,255,.08),transparent 25%),
radial-gradient(circle at bottom right,rgba(212,175,55,.08),transparent 35%),var(--bg);color:var(--text)}
.container{max-width:1400px;margin:auto;padding:30px}
.hero{background:linear-gradient(135deg,#111821,#1b2430);border:1px solid rgba(212,175,55,.25);border-radius:24px;padding:32px;position:relative}
.status{position:absolute;right:25px;top:25px;color:var(--green);font-weight:700}
.brand{font-family:Orbitron,sans-serif;color:var(--gold);letter-spacing:3px}
h1{font-family:Orbitron,sans-serif;font-size:3rem;margin:10px 0}
.subtitle{color:var(--muted)}
.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin:20px 0}
.stat{background:var(--card);padding:20px;border-radius:18px}
.label{font-size:.85rem;color:var(--muted)}
.value{font-size:2rem;color:var(--gold);font-weight:700;margin-top:8px}
.actions{text-align:center;margin:20px 0}
button{border:none;border-radius:30px;padding:14px 26px;font-weight:700;cursor:pointer;margin:5px}
.gen{background:var(--gold)} .clr{background:#27313d;color:#fff}
#loading{text-align:center;color:var(--blue);display:none;padding:15px}
.days{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:18px}
.card{background:var(--card);border-radius:20px;padding:20px}
.day{font-family:Orbitron;color:var(--gold)}
.date{font-size:1.4rem;margin:8px 0}
.time{color:var(--muted);margin-bottom:15px}
.slot{padding:10px;border-radius:10px;margin:8px 0}
.full{background:rgba(46,204,113,.12);border-left:4px solid var(--green)}
.half{background:rgba(212,175,55,.12);border-left:4px solid var(--gold)}
.summary{background:var(--card);border-radius:20px;padding:25px;margin-top:20px}
.row{display:flex;justify-content:space-between;padding:8px 0}
.bar{height:10px;background:#24303d;border-radius:20px;overflow:hidden;width:180px}
.fill{height:100%;background:linear-gradient(90deg,var(--green),var(--gold))}
.footer{text-align:center;color:var(--muted);margin-top:20px}
.empty{color:#667}
</style>
</head>
<body>
<div class="container">
<div class="hero">
<div class="status">● DIGITAL TWIN MODE ONLINE</div>
<div class="brand">PGIL</div>
<h1>SMART CITY OF ROCK</h1>
<div class="subtitle">Real-Time Urban Intelligence @ Rock in Rio Lisboa</div>
</div>

<div class="stats">
<div class="stat"><div class="label">OPERADORES ATIVOS</div><div class="value">5</div></div>
<div class="stat"><div class="label">COBERTURA TOTAL</div><div class="value">120h</div></div>
<div class="stat"><div class="label">ÍNDICE DE EQUILÍBRIO</div><div class="value" id="equity">--</div></div>
</div>

<div class="actions">
<button class="gen" onclick="generate()">⚙ GERAR CENÁRIO</button>
<button class="clr" onclick="drawEmpty()">🧹 LIMPAR</button>
</div>

<div id="loading">A ligar à PGIL...<br>A carregar modelos de inteligência urbana...<br>A otimizar recursos humanos...</div>

<div id="days" class="days"></div>

<div class="summary">
<h2>⚖ Equilíbrio Operacional</h2>
<div id="summary"></div>
</div>

<div class="footer">
PGIL • Plataforma de Gestão Inteligente de Lisboa<br>
Smart City of Rock • Operações de Inteligência Urbana
</div>
</div>

<script>
const people=["António Costa","Angela Gasalho","Paula Melícias","Susana Marques","Valter Ferreira"];

function shuffle(a){let b=[...a];for(let i=b.length-1;i>0;i--){let j=Math.floor(Math.random()*(i+1));[b[i],b[j]]=[b[j],b[i]];}return b;}

function drawEmpty(){
document.getElementById('days').innerHTML=[
["EVENT DAY 01","18 Junho","17h-22h"],
["EVENT DAY 02","20 Junho","13h-23h"],
["EVENT DAY 03","21 Junho","13h-23h"],
["EVENT DAY 04","27 Junho","13h-23h"],
["EVENT DAY 05","28 Junho","13h-23h"]
].map(d=>`<div class="card"><div class="day">${d[0]}</div><div class="date">${d[1]}</div><div class="time">${d[2]}</div><div class="empty">Cenário não gerado</div></div>`).join("");
document.getElementById('summary').innerHTML="";
document.getElementById('equity').innerHTML="--";
}

function generate(){
loading.style.display='block';
setTimeout(()=>{
let best=null,bestScore=999999;

for(let t=0;t<50000;t++){
let hours={}; people.forEach(p=>hours[p]=0);
let sched=[];

sched.push({title:"EVENT DAY 01",date:"18 Junho",time:"17h-22h",slots:[
["Angela Gasalho","full",5],["Paula Melícias","full",5],["Susana Marques","full",5],["Valter Ferreira","full",5]
]});

["20 Junho","21 Junho","27 Junho","28 Junho"].forEach((d,i)=>{
let a=[...people];
if(d==="28 Junho") a=a.filter(x=>x!=="Susana Marques");
a=shuffle(a);
sched.push({title:`EVENT DAY 0${i+2}`,date:d,time:"13h-23h",slots:[
[a[0],"full",10],[a[1],"full",10],[a[2],"half",5]
]});
});

sched.forEach(day=>day.slots.forEach(s=>hours[s[0]]+=s[2]));
let vals=Object.values(hours);
let score=Math.max(...vals)-Math.min(...vals);
if(score<bestScore){bestScore=score;best={sched,hours};}
}

render(best);
loading.style.display='none';
},900);
}

function render(data){
days.innerHTML=data.sched.map(d=>`
<div class="card">
<div class="day">${d.title}</div>
<div class="date">${d.date}</div>
<div class="time">${d.time}</div>
${d.slots.map(s=>`<div class="slot ${s[1]}">${s[0]}${s[1]=='half' ? '<br><small>(REFORÇO)</small>':''}</div>`).join('')}
</div>`).join('');

let max=Math.max(...Object.values(data.hours));
summary.innerHTML=Object.entries(data.hours).sort((a,b)=>b[1]-a[1]).map(([n,h])=>`
<div class="row">
<div>${n} — <strong>${h}h</strong></div>
<div class="bar"><div class="fill" style="width:${(h/max)*100}%"></div></div>
</div>`).join('');

let vals=Object.values(data.hours);
equity.innerHTML=(100-(Math.max(...vals)-Math.min(...vals))*2)+"%";
}

drawEmpty();
</script>
</body>
</html>
